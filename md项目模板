语音分析系统计算单元（zp项目接口机）
===============================

```bash
    语音分析计算单元作为语音分析对外接口层API对外提供数据计算服务，当前版本为开发版本
```

模块包含
-----------
* 接口机模块
    * 接收web服务器请求
* 调度模块
    * 取任务并且与引擎通信计算
* 中间件模块
    * 包含常用工具类、实体类、协议层、数据库操作等

常用配置
---------
* 服务端口配置
* 安全校验配置
* 语音接口协议版本配置
* 推送结果地址配置
    * 预警结果推送地址配置
    * 配置下发结果推送地址配置
* 日志配置
* 数据库配置
* kafka配置
* socket配置

如何使用
------
